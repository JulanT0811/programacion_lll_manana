const titulo = document.getElementById('titulo');
console.log (titulo.textContent);
const nota = document.getElementsByClassName('nota');
array.from(notas).forEach(nota=>console.log(nota.textContent));
const items = document.getElementsByClassName('item');
Array.from(items).forEach(elemento=>console.log(elemento.textContent));
const usuario = document.getElementsByName('usuario');
Array.from(ususarios).forEach(elemento=>console.log(elemento.textContent));